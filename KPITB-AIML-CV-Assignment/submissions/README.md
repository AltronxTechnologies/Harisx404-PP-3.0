# Submission Bundles

Final bundles are generated only after the assignment release gate. Record the
standalone release-repository tag/commit, source parent commit and subtree path,
model/data/config hashes, included files, SHA-256 checksums, and clean-machine
verification. Create no assignment release tag in the shared parent repository.
Never overwrite the exact submitted archive.
